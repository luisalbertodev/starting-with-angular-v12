export interface Product {
  id: string;
  title: string;
  description: string;
  price: number;
  image: string;
}

export interface ItemCart {
  id: string;
  title: string;
  description: string;
  price: number;
  image: string;
  qty: number;
}
